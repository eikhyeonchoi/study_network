## TCP/IP
```
IP: 32bit
DHCP(Dynamic Host Configuration Protocol): IP 자동배정
    네트워크의 모든 주소는 DHCP 서버가 가지고 있다가 클라이언트 PC켜지면서 네트워크에 Boardcast를 뿌림, DHCP서버는 주소하나를 줌
    통신을 하기위해선 TCP/IP 설정이 필요함(IP주소/서브넷마스크, 기본게이트웨이 IP주소, DNS서버의 IP주소)
    이 설정을 DHCP가 자동으로 해줌
```